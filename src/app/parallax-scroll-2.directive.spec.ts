import { ParallaxScroll2Directive } from './parallax-scroll-2.directive';

describe('ParallaxScroll2Directive', () => {
  it('should create an instance', () => {
    const directive = new ParallaxScroll2Directive();
    expect(directive).toBeTruthy();
  });
});
